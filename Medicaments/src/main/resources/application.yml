spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Medication
    username: postgres
    password: khalil
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8082